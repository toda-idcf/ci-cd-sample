<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_importmap_tags %>
  </head>

  <body>
    <h1>application version : <%= Rails.configuration.version %></h1>
    <h2>hostname: <%= `hostname`.strip %></h2>

    <%= yield %>
  </body>
</html>
